#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * _strlen - returns length of string
 *@s : pointer
 * Return: Always 0.
 */

int _strlen(char *s)
{
int leng;
leng = strlen(s);

return (leng);
}
