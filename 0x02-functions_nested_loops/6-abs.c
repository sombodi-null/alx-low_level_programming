#include <stdlib.h>
#include "main.h"
/**
 * _abs - prints the absolute value
 * @a: The character to print
 * Return: On success void.
 */
int _abs(int a)
{
	if (a < 0)
	{
	return (a * -1);
	}
	else
	{
	return (a);
	}
}
